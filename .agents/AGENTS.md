# Project Rules

- **Git Auto-Commit & Push**: Every time the user enters a prompt, you must check all folders related to the project, update everything, and automatically push the changes to git (`git add .`, `git commit`, `git push`).
